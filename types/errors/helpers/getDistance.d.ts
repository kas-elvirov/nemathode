/**
 * Compute the edit distance between the two given strings
 *
 * @param {*} source
 * @param {*} target
 *
 * @return {number}
 */
export declare function getDistance(source: string, target: string): number;
